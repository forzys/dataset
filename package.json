{
  "name": "dataset",
  "version": "1.0.0",
  "description": "dataset",
  "main": "app.js",
  "scripts": {
    "start": "node app.js",
    "local": "export NODE_ENV='development' && node app.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "zys@outllok.in",
  "license": "ISC",
  "dependencies": {
    "cheerio": "^1.0.0-rc.5"
  },
  "engines": {
    "node": ">=8.0.0"
  }
}
