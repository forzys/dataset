{
  "name": "dataset",
  "version": "1.0.0",
  "description": "dataset",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "zys",
  "license": "ISC",
  "dependencies": {
    "cheerio": "^1.0.0-rc.5",
    "request": "^2.88.2"
  },
  "engines": {
    "node": ">=8.0.0"
  }
}
